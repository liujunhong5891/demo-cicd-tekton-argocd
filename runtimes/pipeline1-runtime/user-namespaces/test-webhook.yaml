apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: test-webhook
  namespace: user-pipelines
spec:
  service:
    ports:
      - port: 13000
        targetPort: 13000
  webhook:
    example:
      port: "13000"
      endpoint: /example
      method: POST